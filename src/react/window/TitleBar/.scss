$pm: 5px;
$lh: 20px;

.TitleBar {
    box-sizing: content-box;
    -webkit-app-region: drag;
    user-select: none;
    display: flex;
    background: var(--col-titlebar-bg);
    color: var(--col-text);
    line-height: $lh;
    overflow: hidden;
    * {
        flex: 1;
    }
    .-title {
        color: var(--col-title);
        padding: $pm .5em;
    }
    .-buttons {
        $EIU_W: 3em;
        flex: 0 0 3*$EIU_W;
        -webkit-app-region: none;
        > * {
            padding: $pm 0;
            text-align: center;
            display: inline-block;
            width: $EIU_W;
            
            &:hover { background: var(--col-hovmod-bg); color: var(--col-title); }
            &.-wb-close:hover { background: var(--col-error-bg); color: var(--col-error-text); }
        }
    }
}