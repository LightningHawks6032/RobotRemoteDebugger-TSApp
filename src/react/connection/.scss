
.ConnectionPanel {
    border-bottom: 1px solid var(--col-divider);

    > div {
        display: flex;
        padding: .3em;

        > label, > span, > input, > button {
            flex: 1 1 50px;
            width: 50px;
        }
        > span {
            font-size: .833333em;
        }
        > label, > span {
            line-height: 25px;
        }
    }
}